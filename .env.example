# RESI Subnet 46 Validator Configuration
# Copy to .env and fill in your values:
#   cp .env.example .env
#
# Then source before running:
#   set -a && source .env && set +a
#   python scripts/start_validator.py

# =============================================================================
# REQUIRED - Wallet Configuration
# =============================================================================
WALLET_NAME=validator
WALLET_HOTKEY=default
BITTENSOR_WALLET_PATH=~/.bittensor/wallets

# =============================================================================
# REQUIRED - Pylon Configuration
# =============================================================================
# Generate a secure token: openssl rand -base64 32
PYLON_TOKEN=your_pylon_token_here
# Identity name used by validator (must be in PYLON_IDENTITIES)
PYLON_IDENTITY=validator
# Pylon recognized identities (JSON array)
PYLON_IDENTITIES='["validator"]'
PYLON_URL=http://localhost:8000

# =============================================================================
# Network Configuration (defaults shown)
# =============================================================================
SUBTENSOR_NETWORK=finney
NETUID=46
# Archive network for Pylon historical block lookups (default: finney)
# ARCHIVE_NETWORK=finney
# Blocks older than this use archive node (default: 256)
# ARCHIVE_BLOCKS_CUTOFF=256

# =============================================================================
# Optional - WandB Logging (request API key from subnet owners on Discord)
# =============================================================================
WANDB_API_KEY=your_wandb_api_key_here
WANDB_PROJECT=subnet-46-evaluations-mainnet
WANDB_ENTITY=resi-labs
# WANDB_OFF=false
# WANDB_OFFLINE=false

# =============================================================================
# Optional - Validation Settings (defaults shown, uncomment to override)
# =============================================================================
# LOG_LEVEL=DEBUG
# EPOCH_LENGTH=361
# SCORE_THRESHOLD=0.002
# DISABLE_SET_WEIGHTS=false

# =============================================================================
# Optional - Burn Configuration (defaults shown)
# Protocol burns emissions allocated to subnet owner UID
# =============================================================================
# BURN_AMOUNT=0.5
# BURN_UID=238

# =============================================================================
# Optional - Validation Data Schedule (defaults shown)
# Daily evaluation runs at this time (UTC)
# =============================================================================
# VALIDATION_DATA_URL=https://dashboard.resilabs.ai
# VALIDATION_DATA_SCHEDULE_HOUR=18
# VALIDATION_DATA_SCHEDULE_MINUTE=0
# VALIDATION_DATA_MAX_RETRIES=36
# VALIDATION_DATA_RETRY_DELAY=300

# =============================================================================
# Optional - Model Settings (defaults shown)
# =============================================================================
# MODEL_CACHE_PATH=./model_cache
# MODEL_MAX_SIZE_MB=200
# MODEL_MIN_COMMITMENT_AGE_BLOCKS=8400

# =============================================================================
# Optional - Scheduler Settings (defaults shown)
# =============================================================================
# SCHEDULER_PRE_DOWNLOAD_HOURS=3.0
# SCHEDULER_CATCH_UP_MINUTES=30.0

# =============================================================================
# Optional - Docker Execution (defaults shown)
# =============================================================================
# DOCKER_MEMORY=2g
# DOCKER_CPU=1.0
# DOCKER_TIMEOUT=300
# DOCKER_MAX_CONCURRENT=4
