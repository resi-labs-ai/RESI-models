# Minimal test config for feature encoder tests
version: "1.0.0"

numeric_fields:
  - price
  - beds
  - sqft
  - property_age

boolean_fields:
  - has_pool

feature_transforms: []

feature_order:
  - price
  - beds
  - sqft
  - property_age
  - has_pool
