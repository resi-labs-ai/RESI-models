# Minimal test config for feature encoder tests
version: "1.0.0"

numeric_fields:
  - price
  - beds
  - sqft

feature_transforms:
  - days_since_last_sale

feature_order:
  - price
  - beds
  - sqft
  - days_since_last_sale
