# Config with unregistered transform for testing error handling
version: "1.0.0"

numeric_fields:
  - price

boolean_fields: []

feature_transforms:
  - nonexistent_transform

feature_order:
  - price
  - nonexistent_transform
