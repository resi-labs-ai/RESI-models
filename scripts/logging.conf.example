# logging.conf - Configuration for validator log organization
#
# Copy this file to logging.conf and customize for your environment:
#   cp logging.conf.example logging.conf
#
# This file is sourced by organize_validator_logs.sh

# -----------------------------------------------------------------------------
# Log Output Directory
# -----------------------------------------------------------------------------
# Base directory for organized log files.
# Daily logs will be created in subdirectories:
#   $LOG_BASE_DIR/validator/logs/
#   $LOG_BASE_DIR/validator/errors/

LOG_BASE_DIR="/root/RESI-models/logs"

# -----------------------------------------------------------------------------
# PM2 Log Directory
# -----------------------------------------------------------------------------
# Location of PM2 log files. Default PM2 location is ~/.pm2/logs/
# The validator logs are named: resi-validator-out.log, resi-validator-error.log

PM2_LOG_DIR="/root/.pm2/logs"
